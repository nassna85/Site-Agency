<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
    <a class="navbar-brand" href="#">
        <img src="/images/logo_immo.png" width="50" height="50" class="d-inline-block mr-1" alt="">
        House Agency
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_dashboard') }}">
                    <i class="fas fa-chart-line"></i>
                    DashBoard
                </a>
            </li>
            <li class="nav-item dropdown">
                <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                    <i class="fas fa-hotel"></i>
                    Propriétés
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                    <a class="dropdown-item" href="{{ path('admin_properties_index') }}">Liste des propriétés</a>
                    <a class="dropdown-item" href="#">Liste des options</a>
                    <a class="dropdown-item" href="#">Ajouter une option</a>
                </div>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_comments_index') }}">
                    <i class="fas fa-comments"></i>
                    Commentaires
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_users_index') }}">
                    <i class="fas fa-users"></i>
                    Utilisateurs
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">
                    <i class="fas fa-info"></i>
                    Contact/Proriété
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">
                    <i class="fas fa-envelope"></i>
                    Contact
                </a>
            </li>
        </ul>

        <ul class="navbar-nav ml-auto align-items-center">

            <li class="nav-item">
                <a href="{{ path('homepage') }}" target="_blank" class="nav-link">
                    <i class="fas fa-eye"></i>
                    Site
                </a>
            </li>

            {% if app.user %}

                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountUserDropdownLink">
                        <img src="{% if app.user.avatar %}{{ app.user.avatar }}{% else %}../images/avatar_default.png{% endif %}" class="avatar-mini" alt="{{ app.user.fullName }}"> {{ app.user.fullName }}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountUserDropdownLink">
                        <a class="dropdown-item" href="#">Créer un User/Admin</a>
                        <a class="dropdown-item" href="{{ path('admin_account_logout') }}">Se deconnecter</a>
                    </div>
                </li>

            {% endif %}

        </ul>
    </div>
</nav>

{# SYSTEME DE FLASH #}

{% for label, messages in app.flashes %}
    <div class="container" style="margin-top: 100px;">
        <div class="alert alert-{{ label }}">
            {% for message in messages %}
                <p>{{ message }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}