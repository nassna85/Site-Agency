{% extends 'base.html.twig' %}

{% block title %}Insérer une nouvelle annonce{% endblock %}

{% form_theme form 'properties/_collection.html.twig' %}

{% block body %}

    {# SECTION FORM NEW PROPERTIES #}

    <div class="main-add-property">
        <div class="container">
            <div class="form-content">
                <h2 class="form-title">Insérer votre annonce</h2>

                {{ form_start(form) }}
                
                <div class="row">

                    <div class="col-lg-6">

                        {{ form_row(form.type) }}
                        {{ form_row(form.action) }}
                        {{ form_row(form.city) }}
                        {{ form_row(form.address) }}
                        {{ form_row(form.postalCode) }}
                        {{ form_row(form.price) }}
                        {{ form_row(form.description) }}
                        {{ form_row(form.options) }}

                    </div>

                    <div class="col-lg-6">

                        {{ form_row(form.bedrooms) }}
                        {{ form_row(form.shower) }}
                        {{ form_row(form.area) }}
                        {{ form_row(form.coverImage) }}
                        {{ form_row(form.images) }}

                    </div>
                </div>

                <div class="form-group mt-3">
                    <input type="submit" name="submit" id="submit" class="form-submit" value="Créer votre annonce" />
                </div>

                {{ form_end(form) }}

            </div>
        </div>
    </div>

{% endblock %}

{% block javascripts %}
    <script src="/js/properties.js"></script>
{% endblock %}